# Secret de exemplo para laboratório.
# Em projetos reais, não versionar credenciais reais em repositório Git.
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: storage-lab-config
  labels:
    app.kubernetes.io/name: kubernetes-storage-volumes-lab
    app.kubernetes.io/component: secret-volume-lab
type: Opaque
stringData:
  # Credenciais fictícias para laboratório local.
  # Não utilizar em ambientes reais.
  username: demo-user
  password: demo-password-lab-only
